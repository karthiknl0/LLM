# Note: install torch separately with the right CUDA index URL first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

gradio>=4.44
ollama>=0.4

# Document reading / RAG
chromadb>=0.5
pypdf>=4.0
pandas>=2.0
openpyxl>=3.1

# Vision (video frame extraction)
opencv-python>=4.9
pillow>=10.0

# Voice (speech-to-text + text-to-speech; TTS needs the espeak-ng system package)
faster-whisper>=1.0
kokoro>=0.9
soundfile>=0.12

# Retrieval reranker
sentence-transformers>=3.0

# Screen capture (Screen tab + agent look_at_screen tool)
mss>=9.0

# In-browser verification (agent verify_in_browser tool)
# after pip install, run once: python -m playwright install chromium
playwright>=1.45

# Web research (free search + page extraction, no API keys)
ddgs>=9.0
trafilatura>=1.12

# Image / video generation
diffusers>=0.31
transformers>=4.44
accelerate>=0.33
safetensors>=0.4
sentencepiece>=0.2
imageio[ffmpeg]>=2.34
